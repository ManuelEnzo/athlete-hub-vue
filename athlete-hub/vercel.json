{
  "version": 2,
  "builds": [
    {
      "src": "package.json",
      "use": "@vercel/static-build",
      "config": {
        "distDir": ".output/public"
      }
    }
  ],
  "routes": [
    { "src": "/_nuxt/(.*)", "dest": "/_nuxt/$1" },
    { "src": "/(.*)", "dest": "/index.html" }
  ]
}